<a id="{{$id ?? ''}}" href="{{$url ?? '#'}}" class="btn btn-block btn-sm btn-{{$type ?? ''}} {{$class ?? ''}}" {{$attr ?? ''}}>
    <i class="{{$icon ?? ''}}"></i>
    &nbsp;
    {{$text ?? ''}}
</a>
